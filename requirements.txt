# ProDDoS-NFV: AI-Augmented NFV Orchestration for Proactive DDoS Protection
# Core ML
numpy>=1.21.0
pandas>=1.4.0
scikit-learn>=1.0.0
lightgbm>=3.3.0
joblib>=1.1.0

# Visualization
matplotlib>=3.5.0
seaborn>=0.12.0

# API Server
flask>=2.2.0
requests>=2.28.0

# SDN/NFV (install separately on Linux)
# ryu  # pip install ryu (Linux only, requires eventlet)
# mininet  # sudo apt install mininet (Linux only)
