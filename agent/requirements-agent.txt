# PAD-ONAP SOC Agent dependencies
anthropic>=0.40.0
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
pydantic>=2.7.0
requests>=2.31.0

# GĐ2: RAG Knowledge Base
chromadb>=0.5.0
sentence-transformers>=3.0.0

# Re-use existing pipeline deps (install from project root first)
# pip install -r ../requirements-pipeline.txt
