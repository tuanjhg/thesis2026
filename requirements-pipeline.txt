# PAD-ONAP Pipeline — Native Python Dependencies
# =================================================
# Install into venv before running native pipeline components:
#   python3 -m venv .venv
#   .venv/bin/pip install -r requirements-pipeline.txt
#
# GPU inference (live_pipeline.py) requires CUDA 11.8+.
# If no GPU available, PyTorch CPU build is used automatically.

# ── Kafka ─────────────────────────────────────────────────────────────────────
kafka-python==2.0.2

# ── Numerics (feature extraction + inference) ─────────────────────────────────
numpy>=1.24,<2.0

# ── ML inference ──────────────────────────────────────────────────────────────
xgboost>=2.0.0
scikit-learn>=1.3.0
shap>=0.44.0

# ── Deep learning — install the correct torch variant for your hardware:
#   CUDA 11.8: pip install torch==2.3.0+cu118 --index-url https://download.pytorch.org/whl/cu118
#   CUDA 12.1: pip install torch==2.3.0+cu121 --index-url https://download.pytorch.org/whl/cu121
#   CPU only : pip install torch==2.3.0+cpu    --index-url https://download.pytorch.org/whl/cpu
#
# Default below installs CPU build; override on CUDA server:
torch>=2.0.0

# ── HTTP / utilities ──────────────────────────────────────────────────────────
requests>=2.31.0

# ── Monitoring (metrics_exporter.py) ─────────────────────────────────────────
prometheus-client>=0.19.0
